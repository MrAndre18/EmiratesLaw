$(() => {
  if (!$('.FAQ-exchange__calculator').length) return

  const calculator = $('.FAQ-exchange__calculator'),
        currencies = $('.FAQ-exchange__currencies .currencies-item')
        console.log("🚀 ~ file: exchange-rates.js:6 ~ currencies:", currencies)
})